spring:
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI}

server:
  address: 0.0.0.0
  port: 8080

media:
  base-url: http://localhost:8080/media

  # 🔥 FINAL FALLBACK SYSTEM
  default-image: defaults/exercise.jpg
  default-thumbnail: defaults/exercise_thumb.jpg
  default-animation: defaults/exercise.gif